export class MenuModel {
  id: number;
  background: string;
  name: string;
  constructor(id: number, background: string, name: string) {
    this.id = id;
    this.background = background;
    this.name = name;
  }
}
